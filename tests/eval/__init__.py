"""V3 Phase 6 — evaluation harness.

This package houses the offline-quality evaluation drivers and the
hallucination-injection rig. The eval drivers (round-trip per corpus)
and the inject harness share a single failure-classification
vocabulary so reports stay comparable across runs.
"""
